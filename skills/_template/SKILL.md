# Skill Template

Use this template for reusable skills that interact with NeuroCore capture,
query, or review workflows.

Keep this file named `SKILL.md` inside a `kebab-case` skill directory alongside
`README.md` and `metadata.json`, then run `make validate` or
`python scripts/validate_checkout.py` before treating the skill as complete.
