# Daily Memory Triage

When the operator asks for a quick memory review:

1. Capture any new note with `neurocore capture`.
2. Query recent matching records with `neurocore query`.
3. Summarize the most important stored context, open questions, and next
   actions.
4. If the reference app is running, suggest checking `/dashboard` for recent
   activity.
